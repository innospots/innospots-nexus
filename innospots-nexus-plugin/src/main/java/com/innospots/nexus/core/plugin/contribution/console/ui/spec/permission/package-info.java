/**
 * Permission declarations for pages, nodes, and actions.
 *
 * <p>YAML may use a single code string, an any-of code array, or an object with
 * {@code code} and optional {@code denied} behavior. Front-end permission checks are not a
 * security boundary; API authorization must still be enforced server-side.</p>
 */
package com.innospots.nexus.core.plugin.contribution.console.ui.spec.permission;
