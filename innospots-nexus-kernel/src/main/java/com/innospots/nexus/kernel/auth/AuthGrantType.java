package com.innospots.nexus.kernel.auth;

/**
 * Authentication grant types supported by the kernel boundary.
 */
public enum AuthGrantType {

    PASSWORD,
    TOKEN,
    API_KEY,
    REFRESH_TOKEN
}
