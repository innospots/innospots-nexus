# ---------------------------------------------------------------------------
# 控制台凭据 / TOTP
#
# 绑定: com.innospots.nexus.spring.console.config.ConsoleCredentialTotpProperties
# 装配: ConsoleCredentialConfiguration
# ---------------------------------------------------------------------------
nexus:
  console:
    credential:
      totp:
        # TOTP 密钥加密主密钥；生产必须配置（未配置时相关能力不可用）
        # master-key: "change-me-in-production"
