/**
 * 应用服务启动引导装配。
 *
 * <p>扫描 Core Mapper 并注册 MyBatis-Plus 默认行为；独立服务的数据源、端口与 DDL
 * 在 {@code application.yaml} 中配置，默认值常量见 {@link NexusBootstrapDefaults}。</p>
 */
package com.innospots.nexus.spring.bootstrap;
