/**
 * 插件宿主 Spring Boot 装配。
 *
 * <p>通过 {@link EnableNexusPluginHost} 显式引入，
 * 将 Spring {@link org.springframework.core.env.Environment} 适配为 Core 插件运行时配置。
 * 默认引导见 {@link com.innospots.nexus.spring.bootstrap.EnableNexusAppBootstrap}。</p>
 */
package com.innospots.nexus.spring.plugin.config;
