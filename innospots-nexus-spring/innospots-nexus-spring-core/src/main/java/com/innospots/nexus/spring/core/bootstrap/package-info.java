/**
 * 宿主公共启动引导装配（持久化、启动编排）。
 *
 * <p>归属 {@code innospots-nexus-spring-core}。数据源与演示 DDL 由各 runnable 模块的
 * {@code application.yaml} 与本地 {@code resources} 提供，不在此库写死。</p>
 *
 * @author Smars
 * @date 2026/09/13
 */
package com.innospots.nexus.spring.core.bootstrap;

