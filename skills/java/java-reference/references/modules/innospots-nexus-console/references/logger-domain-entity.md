# 包 `logger.domain.entity`

## AuditLogEntity

**类型：** class

管理控制台操作的仅追加审计日志记录。 可追踪与审查。通过 {@code AuditLogDao} 写入；记录 流程由本包外的拦截器适配器接线。</p>

### 方法

#### `idPrefix() → String`

- **说明：** 返回主键前缀。
- **返回：** 操作结果
