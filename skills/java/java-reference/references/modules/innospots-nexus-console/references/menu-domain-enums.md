# 包 `menu.domain.enums`

## MenuOpenMode

**类型：** enum

打开菜单目标时使用的浏览器目标。

### 枚举常量

| 常量 | 说明 |
|------|------|
| `INTERNAL` | — |
| `NEW_WINDOW` | — |


## MenuType

**类型：** enum

结构与导航菜单节点类型。 授权动作与 API 资源有意归属 权限领域而非本枚举。

### 枚举常量

| 常量 | 说明 |
|------|------|
| `DIRECTORY` | — |
| `PAGE` | — |
| `EXTERNAL_LINK` | — |
