# 审计与调用日志

| 类型 | 说明 |
|------|------|
| `AuditLog` / `AuditLogEntity` | `nx_audit_log` |
| `InvocationLogHandler` | 管理 API 调用管道 |
| `LoggerEntryPlugin` | 内置审计模块入口 |

包级参考：`logger.md`（包 `logger`）、`logger-*.md`。
