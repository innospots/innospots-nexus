# 包 `role.domain.enums`

## RoleBindingSubjectType

**类型：** enum

可绑定到角色的主体。

### 枚举常量

| 常量 | 说明 |
|------|------|
| `USER` | — |
| `ORG_UNIT` | — |


## RoleOwnerType

**类型：** enum

拥有角色定义的层级。

### 枚举常量

| 常量 | 说明 |
|------|------|
| `PLATFORM` | — |
| `TENANT` | — |
| `WORKSPACE` | — |
