# 包 `http`

## HttpClientBuilder

**Type:** class

`CloseableHttpClient` 实例的流式构建器，含合理默认值（连接超时 10 秒、响应超时 30 秒、启用重定向）。

## HttpResult

**Type:** record

HTTP 请求的不可变结果。

## HttpUtils

**Type:** class

HTTP GET 与 POST（JSON）请求的便捷方法。
