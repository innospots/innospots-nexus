# 包 `mapstruct`

## BaseBeanConverter

**Type:** interface

领域模型与持久化实体之间的基础 MapStruct 风格转换器接口。

## BaseMapperConfig

**Type:** interface

所有领域映射器共享的 MapStruct 映射器配置。

## BaseMapperSupport

**Type:** class

通过映射函数转换集合的工具。
