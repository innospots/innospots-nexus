# 包 `resource.domain.entity`

## MetaResourceEntity

**类型：** class

已存储文件元数据的持久化实体（按 OwnershipEntity 归属列隔离）。

### 成员变量

| 名称 | 类型 | 说明 |
|------|------|------|
| `private long` | `文件大小（字节）。` | 文件大小（字节）。 private long fileSize; |
