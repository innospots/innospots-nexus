# 包 `quartz.enums`

## TimePeriod

**类型：** enum

Quartz Cron 转换使用的常见调度周期单位。

### 枚举常量

| 常量 | 说明 |
|------|------|
| `MINUTE` | 按分钟间隔重复，例如每 5 分钟。 |
| `HOUR` | 按小时间隔或一天内选定小时重复。 |
| `DAY` | 按天间隔重复，通常在固定时刻触发。 |
| `WEEK` | 在选定星期几重复，通常在固定时刻触发。 |
| `MONTH` | 在选定月中日期重复，通常在固定时刻触发。 |
