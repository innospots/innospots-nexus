package com.innospots.nexus.core.plugin.config;

/** Supported plugin configuration value types. */
public enum ConfigType {
    STRING,
    INTEGER,
    LONG,
    BOOLEAN,
    DURATION,
    SECRET
}
